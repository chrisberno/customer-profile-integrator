import React, { FC } from "react";
export interface AISummary {
    enabled: boolean;
    summary: string | undefined;
    isLoading: boolean;
    summaryInput: string;
    setSummaryInput: (summary: string) => void;
    loadingIndicator: () => React.ReactNode;
    error?: Error;
}
export interface BasePreTransferFormProps {
    onCancelClick: () => void;
    isNotesFFEnabled: boolean;
    isAISummaryEnabled?: boolean;
    transferStarted: boolean;
    handleTransfer: (notes: string, summary?: string) => void;
    aiSummary: AISummary;
}
export declare const BasePreTransferForm: FC<BasePreTransferFormProps>;
