import { IQueue, IWorker, ThemeProps } from "../../../utils";
export interface BaseWorkerDirectoryState {
    workers: Array<IWorker>;
    queues: Array<IQueue>;
    isWorkerLoading: boolean;
    isQueueLoading: boolean;
    agentSelectedForTransfer: IWorker | null;
    queueSelectedForTransfer: IQueue | null;
    transferErrorWorker?: string | null;
    transferErrorWorkerType?: "error" | "warning" | null;
    transferErrorQueue?: string | null;
}
export interface WorkerDirectoryState extends Omit<BaseWorkerDirectoryState, "workers" | "queues" | "isWorkerLoading" | "isQueueLoading">, ThemeProps {
    isTransferDialpadLoading: boolean;
}
