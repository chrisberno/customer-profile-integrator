import { CSSProps } from "../../utils/theme";
export declare enum BadgeVariants {
    standard = "standard",
    dot = "dot",
    danger = "danger"
}
export interface BaseBadgeChildrenProps {
    invisible: boolean;
    max: number;
    showZero: boolean;
    variant: BadgeVariants;
    children: number;
}
export declare enum BaseBadgeChildrenKeys {
    defaultCircle = "defaultCircle"
}
interface BaseBadgePosition {
    top?: string;
    right?: string;
}
export interface BaseBadgeProps extends BaseBadgeChildrenProps {
    programmable: boolean;
    themeOverride: Partial<BadgeThemeProps>;
    position?: BaseBadgePosition;
}
export interface BadgeThemeProps {
    OuterCircle: CSSProps;
    InnerCircle: CSSProps;
}
export {};
