import * as React from "react";
import { DynamicContentStore } from "../../utils";
import { BaseBadgeChildrenKeys, BaseBadgeProps, BadgeVariants } from "./BaseBadge.definitions";
export declare class BaseBadge extends React.PureComponent<BaseBadgeProps> {
    static readonly variants: typeof BadgeVariants;
    static readonly displayName = "Badge";
    static readonly Content: DynamicContentStore<BaseBadgeProps, BaseBadgeChildrenKeys>;
    static defaultProps: {
        invisible: boolean;
        showZero: boolean;
        variant: BadgeVariants;
        programmable: boolean;
        themeOverride: {};
        max: number;
    };
    render(): JSX.Element;
    renderContent(): string;
    getWrapperComponent(): {
        Component: any;
        props: object;
    };
}
