import styled from "@emotion/styled";
import { CommonTheme } from "./emotion";
export interface ThemeSupport {
    theme?: Partial<CommonTheme>;
    className?: string;
}
export interface BaseThemeColors {
    holdColor: string;
    declineColor: string;
    acceptColor: string;
    declineTextColor: string;
    acceptTextColor: string;
    completeTaskColor: string;
    flexBlueColor: string;
    agentAvailableColor: string;
    agentBusyColor: string;
    agentUnavailableColor: string;
    agentOfflineColor: string;
}
export interface ChannelColors {
    inactive: string;
    call: string;
    video: string;
    sms: string;
    chat: string;
    facebook: string;
    googleBusinessMessages: string;
    line: string;
    whatsapp: string;
    appleMessagesForBusiness: string;
    email: string;
    custom: string;
}
export declare const getBackgroundWithHoverCSS: (background: string | number, lightHover: boolean, noHoverSelector?: boolean, disabled?: boolean) => string;
export declare function getHover(background: string | number, lightHover: boolean): any;
export { styled };
