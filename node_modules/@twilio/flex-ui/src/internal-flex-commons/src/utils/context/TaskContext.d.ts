import { VoiceCall } from "@twilio/flex-sdk";
import { BaseITask, BaseTaskChannelDefinition } from "../models";
export interface BaseTaskContextProps {
    task?: BaseITask;
    voiceCall?: VoiceCall;
    channelDefinition?: BaseTaskChannelDefinition;
}
