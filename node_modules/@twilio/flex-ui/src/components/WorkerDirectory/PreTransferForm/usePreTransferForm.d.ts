import { IParticipant, IQueue, ITask, IWorker } from "../../../models";
import { AISummary } from "./types";
interface PreTransferFormProps {
    worker?: IWorker;
    queue?: IQueue;
    task?: ITask;
    agent: Pick<IParticipant, "participantSid"> | undefined;
    isNotesFFEnabled?: boolean;
    setError: (error: string) => void;
    aiSummary?: AISummary;
}
export declare function usePreTransferForm({ worker, queue, task, agent, setError, isNotesFFEnabled, aiSummary }: PreTransferFormProps): {
    transferStarted: boolean;
    handleTransfer: (notes: string, summary?: string) => Promise<void>;
};
export {};
