import { FlexError, ITask } from "../../..";
import { AISummary } from "./types";
export declare function useAISummary(task?: ITask, participantSid?: string, setError?: (error: string, type?: "error" | "warning") => void): {
    summary: AISummary;
    isLoading: boolean;
    error: FlexError;
};
