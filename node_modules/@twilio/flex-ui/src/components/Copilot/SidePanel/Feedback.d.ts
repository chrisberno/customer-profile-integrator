import { AiCopilotAssistApiReference } from "../../../core/FlexDataClient";
import { ITask } from "../../../models";
import { FeatureType } from "../telemetry/events";
export declare const Feedback: ({ feature, id, references, interactionSid, channelSid, task, conversationSid }: {
    feature: FeatureType;
    id: string;
    references?: AiCopilotAssistApiReference[];
    interactionSid: string;
    channelSid: string;
    task?: ITask;
    conversationSid?: string;
}) => JSX.Element;
