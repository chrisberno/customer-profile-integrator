import { EventEmitter } from "events";
import { FlexSSOUrls, OAuthConfig } from "../state/AppConfig";
import { ConsoleLoginStorageParams } from "./ConsoleLoginStorage";
import { ConsoleLoginParams } from "./OAuthLoginHandler.definitions";
type MergedOAuthConfig = OAuthConfig & FlexSSOUrls;
type ConsoleLoginProps = ConsoleLoginParams | ConsoleLoginStorageParams | undefined;
export declare class OAuthLoginHandler extends EventEmitter {
    private _config;
    private _ssoUrls;
    private _queryParams;
    private constructor();
    static create(config?: MergedOAuthConfig, consoleLoginParams?: ConsoleLoginProps): Promise<OAuthLoginHandler>;
    /**
     * Tries to log in with provided information. Does not initialize token expiration checker.
     * @param {OAuthConfig} config OAuth2.0 configuration object
     * @param {ConsoleLoginProps} consoleLoginParams Optional parameters passed by Twilio Console
     * @returns {Promise<boolean>} Returns a promise that resolves to a boolean
     * @example
     * import { OAuthLoginHandler } from "@twilio/flex-ui";
     * OAuthLoginHandler.tryLogin({ clientId: "clientId", connection: "connectionId", loginPopup: true});
     */
    static tryLogin(config?: MergedOAuthConfig, consoleLoginParams?: ConsoleLoginProps): Promise<boolean>;
    private initialize;
    private tryLoginInNewWindow;
    private getToken;
    private getProofKeyFromStorageOrCreate;
    private getAuthorizeUrl;
    private openLoginWindow;
    private isInIFrame;
    private isInLoginWindow;
    private coerceQueryStringToString;
    private parsePayloadFromQueryString;
}
export {};
