'use strict';;let utils,defaults,AxiosHeaders;_13a‍.x([["default",()=>transformData]]);_13a‍.w("./../utils.js",[["default",["utils"],function(v){utils=v}]]);_13a‍.w("../defaults/index.js",[["default",["defaults"],function(v){defaults=v}]]);_13a‍.w("../core/AxiosHeaders.js",[["default",["AxiosHeaders"],function(v){AxiosHeaders=v}]]);





/**
 * Transform the data for a request or a response
 *
 * @param {Array|Function} fns A single function or Array of functions
 * @param {?Object} response The response object
 *
 * @returns {*} The resulting transformed data
 */
               function transformData(fns, response) {
  const config = this || defaults;
  const context = response || config;
  const headers = AxiosHeaders.from(context.headers);
  let data = context.data;

  utils.forEach(fns, function transform(fn) {
    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);
  });

  headers.normalize();

  return data;
}
